apiVersion: v1
kind: Service
metadata:
  name: ocr-gateway-service
  labels:
    app: ocr-gateway
spec:
  selector:
    app: ocr-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
