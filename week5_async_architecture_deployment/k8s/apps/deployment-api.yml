apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocr-api-deployment
  labels:
    app: ocr-api
spec:
  replicas: 1 # Lightweight Rust API on Redis node pool
  selector:
    matchLabels:
      app: ocr-api
  template:
    metadata:
      labels:
        app: ocr-api
    spec:
      nodeSelector:
        kubernetes.azure.com/agentpool: cpunp
      tolerations:
        - key: "sku"
          operator: "Equal"
          value: "cpunp"
          effect: "NoSchedule"
      containers:
        - name: ocr-api-rust
          image: acrtnmweek3.azurecr.io/ocr-api-rust:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: ocr-pipeline-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocr-worker-rt-deployment
  labels:
    app: ocr-worker-rt
spec:
  replicas: 0 # KEDA scales this on T4 nodes
  selector:
    matchLabels:
      app: ocr-worker-rt
  template:
    metadata:
      labels:
        app: ocr-worker-rt
    spec:
      nodeSelector:
        kubernetes.azure.com/agentpool: gpunpt4
      tolerations:
        - key: "sku"
          operator: "Equal"
          value: "gpunpt4"
          effect: "NoSchedule"
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: "4Gi"
        - name: model-weights
          persistentVolumeClaim:
            claimName: model-weights-pvc
      containers:
        - name: ocr-worker-rt-python
          image: acrtnmweek3.azurecr.io/ocr-worker-rt:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: ocr-pipeline-config
          resources:
            requests:
              cpu: "4"
              memory: "16Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "14"
              memory: "32Gi"
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: dshm
              mountPath: "/dev/shm"
            - name: model-weights
              mountPath: "/mnt/models"
              readOnly: true
